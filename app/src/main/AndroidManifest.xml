<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hasan.loginregister">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SPLASH">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".UserAreaActivity" />
        <!-- <activity android:name=".Calendar" /> -->
        <activity android:name=".Sections" />
        <activity android:name=".Students" />
        <activity android:name=".SectionA" />
        <!-- <activity android:name=".SecB" /> -->
        <activity android:name=".SectionB" />
        <activity android:name=".SectionC" />
        <activity android:name=".SectionD" />
        <activity android:name=".PickingD" />
        <activity android:name=".AdminLogin"></activity>
    </application>

</manifest>